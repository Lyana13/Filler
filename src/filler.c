#include "filler.h"

void initial_status(t_filler **state, char *programs)
{
	char *str;
	state = malloc()
}

int main(int argc, char **argv)
{
	t_filler	*state;
	t_point		*pos;
	char *str;
	int i;

	initial_status(*str, argv[0]);
	
	return (0);
}